#!/usr/bin/env bash
# thin wrapper: run ctl.py with the sleepy venv python
INSTALL_PATH="${SLEEPY_HOME:-$HOME/.local/share/sleepy}"
exec "$INSTALL_PATH/venv/bin/python" "$INSTALL_PATH/sleepy/ctl.py" "$@"