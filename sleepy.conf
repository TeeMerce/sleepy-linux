# sleepy configuration (KEY=VALUE)

# --- TV ---
TV_IP=10.10.10.12           # <-- CHANGE: your TV's IP
TV_MAC=20:28:bc:71:fd:56     # <-- your TV's MAC (from v1)

# --- WOL (wake) ---
WOL_REPEATS=3
WOL_INTERVAL_MS=200

# --- RGB (OpenRGB one-shot, no background server) ---
OPENRGB_BIN=openrgb
RGB_ON_PROFILE=On
RGB_OFF_PROFILE=Off

# --- listener behaviour ---
DEBOUNCE_SECONDS=3
POLL_SECONDS=1