# sleepy configuration (KEY=VALUE)
# Keep value lines bare — no inline comments.

# --- TV ---
TV_IP=10.10.10.12
TV_MAC=20:28:bc:71:fd:56

# --- WOL (wake) ---
WOL_REPEATS=3
WOL_INTERVAL_MS=250

# --- RGB (OpenRGB one-shot, no background server) ---
OPENRGB_BIN=openrgb
RGB_ON_PROFILE=On
RGB_OFF_PROFILE=Off

# --- listener behaviour ---
GUARD_SECONDS=4
LOCK_POLL_SECONDS=2